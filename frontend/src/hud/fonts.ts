/**
 * Centralized font URLs for drei <Text> instances.
 *
 * Intel One Display Bold is hosted on the onlinewebfonts CDN. troika-three-text
 * accepts any URL pointing to a real TTF/OTF/WOFF file. The same CDN entry is
 * referenced by an `@font-face` declaration in `src/index.css` so DOM elements
 * with `font-silicon` class fall back to the same typeface.
 */
export const SILICON_FONT_URL =
  'https://db.onlinewebfonts.com/t/f41c652afbe4e65a3ab9b69ca48b1fee.ttf'
